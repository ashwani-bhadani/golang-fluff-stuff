package main

/*
	go get -u github.com/gorilla/mux
	go get go.mongodb.org/mongo-driver/v2/mongo
	go get github.com/joho/godotenv //to store safely credentials in .env file
	in an app in root it is expected that there is just one go file main.go, rest goes in to sub-directories
*/
